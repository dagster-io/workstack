# REQUIRED REPOSITORY SETTING:
# Settings > Actions > General > Workflow permissions
# ✓ Enable "Allow GitHub Actions to create and approve pull requests"
#
# Without this setting, PR creation will fail with:
# "GitHub Actions is not permitted to create or approve pull requests"

name: Dispatch Erk Queue (Git Only)
run-name: "${{ inputs.issue_number }}:${{ inputs.distinct_id }}"

on:
  workflow_dispatch:
    inputs:
      issue_number:
        description: "GitHub issue number to implement"
        required: true
        type: string
      submitted_by:
        description: "GitHub username of the person who submitted the issue"
        required: true
        type: string
      distinct_id:
        description: "Unique identifier for run discovery (base36)"
        required: true
        type: string
      issue_title:
        description: "Issue title for workflow run display"
        required: true
        type: string

concurrency:
  group: implement-issue-${{ github.event.inputs.issue_number }}
  cancel-in-progress: true

jobs:
  implement:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
    steps:
      # =================================================================
      # PHASE 1: CHECKOUT AND SETUP
      # =================================================================
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ERK_QUEUE_GH_PAT }}
          fetch-depth: 0

      - name: Setup all tools
        run: |
          # Install uv
          curl -LsSf https://astral.sh/uv/install.sh | sh
          source $HOME/.cargo/env

          cd $GITHUB_WORKSPACE

          # Install workspace packages in dependency order
          uv tool install --from ./packages/dot-agent-kit --with ./packages/erk-shared dot-agent-kit
          uv tool install --from . --with ./packages/erk-shared --with ./packages/dot-agent-kit erk

          # Install Claude Code (curl-based, newer pattern)
          curl -fsSL https://claude.ai/install.sh | bash

          # Install Prettier via npm (no Graphite CLI needed)
          npm install -g prettier

          # Install erk kit for Claude Code
          dot-agent kit install erk

      - name: Configure git
        env:
          SUBMITTED_BY: ${{ inputs.submitted_by }}
        run: |
          git config user.name "$SUBMITTED_BY"
          git config user.email "${SUBMITTED_BY}@users.noreply.github.com"

      - name: Detect trunk branch
        id: trunk
        run: |
          if git ls-remote --heads origin main | grep -q main; then
            echo "trunk_branch=main" >> $GITHUB_OUTPUT
            echo "Detected trunk branch: main"
          elif git ls-remote --heads origin master | grep -q master; then
            echo "trunk_branch=master" >> $GITHUB_OUTPUT
            echo "Detected trunk branch: master"
          else
            echo "❌ Could not detect trunk branch (main or master)"
            exit 1
          fi

      # =================================================================
      # PHASE 2: BRANCH SETUP
      # =================================================================
      - name: Find existing PR for issue
        id: find_pr
        env:
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          GH_TOKEN: ${{ github.token }}
        run: |
          # Find PR that closes this issue (created by erk submit)
          PR_DATA=$(gh pr list --state open --json number,headRefName --search "Closes #$ISSUE_NUMBER" | jq -r '.[0] // empty')

          if [ -n "$PR_DATA" ]; then
            PR_NUMBER=$(echo "$PR_DATA" | jq -r '.number')
            BRANCH_NAME=$(echo "$PR_DATA" | jq -r '.headRefName')
            echo "branch_name=$BRANCH_NAME" >> $GITHUB_OUTPUT
            echo "pr_number=$PR_NUMBER" >> $GITHUB_OUTPUT
            echo "pr_exists=true" >> $GITHUB_OUTPUT
            echo "Found existing PR #$PR_NUMBER for branch $BRANCH_NAME"
          else
            echo "pr_exists=false" >> $GITHUB_OUTPUT
            echo "No existing PR found for issue #$ISSUE_NUMBER"
            echo "Error: erk submit should have created a PR before dispatching workflow"
            exit 1
          fi

      - name: Checkout implementation branch
        env:
          BRANCH_NAME: ${{ steps.find_pr.outputs.branch_name }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          GH_TOKEN: ${{ github.token }}
        run: |
          git fetch origin "$BRANCH_NAME"
          git checkout "$BRANCH_NAME"
          echo "Checked out branch: $BRANCH_NAME"

      - name: Reconstruct .impl/ from GitHub issue
        env:
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          GH_TOKEN: ${{ github.token }}
        run: |
          source $HOME/.cargo/env
          ISSUE_TITLE=$(gh issue view "$ISSUE_NUMBER" --json title -q .title)

          # Remove stale .impl/ if exists (for reruns)
          rm -rf .impl

          # Reconstruct from issue (single source of truth)
          dot-agent run erk create-impl-from-issue "$ISSUE_NUMBER" "$ISSUE_TITLE"

          if [ $? -ne 0 ]; then
            gh issue comment "$ISSUE_NUMBER" --body "❌ **Plan Reconstruction Failed**

          Could not fetch plan content for issue #$ISSUE_NUMBER."
            exit 1
          fi

          # Add run-info.json
          cat > .impl/run-info.json <<EOF
          {
            "run_id": "${{ github.run_id }}",
            "run_url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          }
          EOF
          echo "✓ Created .impl/ from GitHub issue #$ISSUE_NUMBER"

      # =================================================================
      # PHASE 3: PR SETUP
      # =================================================================
      - name: Use existing PR
        id: use_pr
        env:
          PR_NUMBER: ${{ steps.find_pr.outputs.pr_number }}
          BRANCH_NAME: ${{ steps.find_pr.outputs.branch_name }}
        run: |
          echo "pr_number=$PR_NUMBER" >> $GITHUB_OUTPUT
          echo "Using existing PR #$PR_NUMBER for branch $BRANCH_NAME"

      - name: Post workflow started comment
        env:
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          BRANCH_NAME: ${{ steps.find_pr.outputs.branch_name }}
          PR_NUMBER: ${{ steps.use_pr.outputs.pr_number }}
          GH_TOKEN: ${{ github.token }}
        run: |
          STARTED_AT=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          WORKFLOW_RUN_ID="${{ github.run_id }}"
          WORKFLOW_RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"

          cat > comment_body.md <<EOF
          ⚙️ GitHub Action Started

          <details>
          <summary>📋 Metadata</summary>

          <!-- erk:metadata-block:workflow-started -->
          \`\`\`yaml
          schema: workflow-started
          status: started
          started_at: $STARTED_AT
          workflow_run_id: "$WORKFLOW_RUN_ID"
          workflow_run_url: $WORKFLOW_RUN_URL
          branch_name: $BRANCH_NAME
          issue_number: $ISSUE_NUMBER
          \`\`\`
          <!-- /erk:metadata-block:workflow-started -->

          </details>

          ---

          Setup completed successfully.

          **Branch:** \`$BRANCH_NAME\`
          **PR:** [#$PR_NUMBER](https://github.com/${{ github.repository }}/pull/$PR_NUMBER)
          **Status:** Ready for implementation

          [View workflow run]($WORKFLOW_RUN_URL)
          EOF

          gh issue comment "$ISSUE_NUMBER" --body-file comment_body.md
          rm -f comment_body.md

      - name: Update issue body with dispatch info
        env:
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          GH_TOKEN: ${{ github.token }}
        run: |
          source $HOME/.cargo/env
          WORKFLOW_RUN_ID="${{ github.run_id }}"
          DISPATCHED_AT=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          if dot-agent run erk update-dispatch-info "$ISSUE_NUMBER" "$WORKFLOW_RUN_ID" "$DISPATCHED_AT"; then
            echo "✅ Updated issue body with dispatch info"
          else
            echo "⚠️ Issue does not have plan-header block - skipping update"
          fi

      # =================================================================
      # PHASE 4: IMPLEMENTATION
      # =================================================================
      - name: Run implementation
        id: implement
        run: |
          set +e

          claude --print \
            --model claude-opus-4-5-20251101 \
            --output-format stream-json \
            --dangerously-skip-permissions \
            --verbose \
            "/erk:plan-implement"

          EXIT_CODE=$?
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT

          if [ $EXIT_CODE -eq 0 ]; then
            echo "implementation_success=true" >> $GITHUB_OUTPUT
          else
            echo "implementation_success=false" >> $GITHUB_OUTPUT
          fi

          exit 0
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GH_TOKEN: ${{ github.token }}

      # =================================================================
      # PHASE 5: SUBMISSION (Pure Git - no Graphite)
      # =================================================================
      - name: Submit branch with proper commit message
        id: submit
        if: steps.implement.outputs.implementation_success == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          BRANCH_NAME: ${{ steps.find_pr.outputs.branch_name }}
          SUBMITTED_BY: ${{ inputs.submitted_by }}
        run: |
          # Configure git author
          git config user.name "$SUBMITTED_BY"
          git config user.email "$SUBMITTED_BY@users.noreply.github.com"

          # Stage all implementation changes
          git add -A

          # Submit with pure git - AI generates commit message from diff
          claude --print \
            --model claude-opus-4-5-20251101 \
            --output-format stream-json \
            --dangerously-skip-permissions \
            --verbose \
            "/git:pr-push Implement issue #$ISSUE_NUMBER"

          # Capture implementation commit SHA
          echo "impl_sha=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT

      - name: Mark PR ready for review
        if: steps.implement.outputs.implementation_success == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH_NAME: ${{ steps.find_pr.outputs.branch_name }}
        run: |
          gh pr ready "$BRANCH_NAME"
          echo "✅ PR marked ready for review"

      - name: Update PR body with implementation summary
        if: steps.implement.outputs.implementation_success == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH_NAME: ${{ steps.find_pr.outputs.branch_name }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          SUBMITTED_BY: ${{ inputs.submitted_by }}
          IMPL_SHA: ${{ steps.submit.outputs.impl_sha }}
        run: |
          # Get the implementation commit message using the captured SHA
          commit_body=$(git log "$IMPL_SHA" -1 --pretty=%B)

          # Get PR number for checkout instructions
          PR_NUMBER=$(gh pr view "$BRANCH_NAME" --json number -q .number)

          # Get standardized metadata footer from get-pr-metadata
          pr_metadata=$(dot-agent run erk get-pr-metadata --pr-number "$PR_NUMBER" 2>/dev/null || echo "")

          # Build PR body: summary + footer
          cat > pr_body.md <<BODY_EOF
          ## Summary

          $commit_body
          $pr_metadata
          BODY_EOF

          gh pr edit "$BRANCH_NAME" --body-file pr_body.md
          rm -f pr_body.md
          echo "✅ PR body updated with implementation summary"

      - name: Trigger CI workflows
        if: steps.implement.outputs.implementation_success == 'true'
        env:
          BRANCH_NAME: ${{ steps.find_pr.outputs.branch_name }}
          SUBMITTED_BY: ${{ inputs.submitted_by }}
        run: |
          # Configure git author
          git config user.name "$SUBMITTED_BY"
          git config user.email "$SUBMITTED_BY@users.noreply.github.com"

          # Push empty commit to trigger CI via push event
          # Using PAT from checkout step allows triggering other workflows
          git commit --allow-empty -m "Trigger CI workflows"
          git push origin "$BRANCH_NAME"
          echo "✅ CI workflows triggered via push event"
